<h1><%= Report.human_attribute_name(:editing_report) %></h1>
<% if @report.present? %>
  <%= render 'form', report: @report %>
<% else %>
  <%= render partial: 'error_messages', locals: { message: '編集' } %>
<% end %>

<%= link_to t('views.common.show'), @report %> |
<%= link_to t('views.common.back'), reports_path %>
